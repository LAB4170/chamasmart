---
phase: 40
plan: 1
wave: 1
---

# Plan 40.1: Backend API Sync & Stabilization

## Objective
Implement missing endpoints in `GovernanceController`, `WelfareController`, and `AuthController` to sync with frontend expectations. Adjust frontend `axios.js` to intelligently handle 401/404 errors and stop logout loops.

## Context
- .gsd/ROADMAP.md
- backend/src/main/java/com/chamasmart/backend/controller/GovernanceController.java
- backend/src/main/java/com/chamasmart/backend/controller/WelfareController.java
- backend/src/main/java/com/chamasmart/backend/controller/AuthController.java
- frontend/src/services/axios.js

## Tasks

<task type="auto">
  <name>Sync Governance & Meetings Endpoints</name>
  <files>
    - backend/src/main/java/com/chamasmart/backend/controller/GovernanceController.java
  </files>
  <action>
    - Add `@GetMapping("/chamas/{chamaId}/meetings/{id}")`
    - Add `@PutMapping("/chamas/{chamaId}/meetings/{id}")`
    - Add `@PostMapping("/chamas/{chamaId}/meetings/{id}/attendance")`
    - Add `@PostMapping("/chamas/{chamaId}/meetings/{id}/publish")`
    - Add `@DeleteMapping("/chamas/{chamaId}/meetings/{id}")`
    - Return mock data or minimal `ResponseEntity.ok(ApiResponse.success(null, "Success"))` if full logic is missing, as the primary goal is API surface sync to unblock the frontend.
  </action>
  <verify>grep -q "meetings/{id}" backend/src/main/java/com/chamasmart/backend/controller/GovernanceController.java && echo "PASS"</verify>
  <done>Meeting CRUD endpoints are present in the GovernanceController.</done>
</task>

<task type="auto">
  <name>Sync Welfare & Emergency Drives Endpoints</name>
  <files>
    - backend/src/main/java/com/chamasmart/backend/controller/WelfareController.java
  </files>
  <action>
    - Add `@PostMapping("/chamas/{chamaId}/contributions")`
    - Add `@GetMapping("/{chamaId}/emergency-drives")`
    - Add `@PostMapping("/{chamaId}/emergency-drives")`
    - Add `@PostMapping("/emergency-drives/{driveId}/contribute")`
    - Add `@GetMapping("/{chamaId}/ledger")`
    - Ensure appropriate `ApiResponse.success(null, ...)` returns.
  </action>
  <verify>grep -q "emergency-drives" backend/src/main/java/com/chamasmart/backend/controller/WelfareController.java && echo "PASS"</verify>
  <done>Welfare contributions and emergency drive endpoints are present.</done>
</task>

<task type="auto">
  <name>Sync Auth Verification Endpoints</name>
  <files>
    - backend/src/main/java/com/chamasmart/backend/controller/AuthController.java
  </files>
  <action>
    - Add `@PostMapping("/verify-email")`
    - Add `@PostMapping("/verify-phone")`
    - Add `@PostMapping("/resend-email-verification")`
    - Add `@PostMapping("/resend-phone-verification")`
  </action>
  <verify>grep -q "verify-email" backend/src/main/java/com/chamasmart/backend/controller/AuthController.java && echo "PASS"</verify>
  <done>Auth verification endpoints are present.</done>
</task>

<task type="auto">
  <name>Fix Frontend Axios Logout Loop</name>
  <files>
    - frontend/src/services/axios.js
  </files>
  <action>
    - In the `401 Unauthorized` interceptor, handle missing endpoints properly so they do not force a logout.
    - If it's a 401 but the `error.response.data.message` indicates a missing route (or if it's actually a 404 disguised as 401 by Spring Security), prevent standard logout.
    - Alternatively, just log the error to the console when `error.config.url` matches unknown or missing API paths without removing token. 
    - The actual goal is to ensure the user stays logged in if they navigate to a missing feature.
  </action>
  <verify>grep -q "suppressing logout" frontend/src/services/axios.js || echo "Check manually"</verify>
  <done>Axios does not aggressively wipe the user token on unexpected 401s.</done>
</task>

## Success Criteria
- [ ] Governance endpoints return 200 OK
- [ ] Welfare endpoints return 200 OK
- [ ] Auth endpoints return 200 OK
- [ ] Axios does not log out users on 401/404s
