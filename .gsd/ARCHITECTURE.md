# Architecture

> Auto-generated by /map on 2026-03-02

## Overview

ChamaSmart is a financial management platform for investment groups (Chamas). It supports various group saving and lending models including ROSCA (Rotating Savings and Credit Association) and ASCA (Accumulated Savings and Credit Association), managing contributions, loans, dividends, and meetings.

```
┌─────────────────────────────────────────┐
│           React Frontend (Vite)         │
├─────────────────────────────────────────┤
│        Node.js / Express Backend        │
├─────────────────────────────────────────┤
│    PostgreSQL (DB) + Firebase (Auth)    │
└─────────────────────────────────────────┘
```

## Components

### Backend (Express API)
- **Purpose:** Core business logic, data persistence, and integration with Firebase/Redis.
- **Location:** `backend/`
- **Modules:**
  - `controllers/`: Handles request logic (e.g., `chamaController.js`, `loanController.js`).
  - `routes/`: Defines API endpoints (e.g., `chamas.js`, `loans.js`).
  - `models/`: Database schema definitions (PostgreSQL).
  - `middleware/`: Auth, error handling, and security.
  - `config/`: Database, Firebase, and Redis configurations.

### Frontend (React)
- **Purpose:** User interface for managing Chamas, loans, and personal profiles.
- **Location:** `frontend/`
- **Structure:**
  - `src/pages/`: Main views (Auth, Dashboard, Chama management).
  - `src/components/`: Reusable UI components (UI library, Layout).
  - `src/hooks/`: Custom React hooks for data fetching and state.

## Data Flow

1. **Authentication:** User logs in via Firebase (Frontend) -> Token sent to Backend -> Verified via Firebase Admin SDK -> Session/JWT established.
2. **API Requests:** Frontend calls Express endpoints -> Controller validates data -> Database/Service layer executes transaction -> Result returned to Frontend.
3. **Real-time/Async:** Potential integration with Firebase/Redis for notifications or caching.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Firebase | Auth/SDK | User authentication and synchronization |
| PostgreSQL | Database | Primary relational data storage |
| Redis | Cache | (Detected in config) Potential session/data caching |

## Technical Debt

- [ ] Multiple `authController` versions (`authController.js` vs `authController.backup.js`).
- [ ] Mixed error handling patterns in `middleware/`.
- [ ] Potential lack of automated test coverage (tests directory not prominent).
- [ ] Large controller files (e.g., `loanController.js` at 45KB) suggest need for refactoring.

## Conventions

**Naming:** PascalCase for React components, camelCase for variables/functions.
**Structure:** Feature-based directory nesting in frontend (`pages/chama/core`, `pages/chama/rosca`).
**Testing:** Minimal evidence of unit/integration tests found in root scan.
